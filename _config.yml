# Site settings
title: "Jay Wong — Solving complexity with design"
description: "Experience designer specialising in inclusive, AI-led and service-driven products"

# GitHub Pages settings
# If your site is at https://username.github.io/repository-name, set baseurl to "/repository-name"
# If your site is at https://username.github.io, leave baseurl as ""
baseurl: "/jay.wong" # Change this if your site is in a subdirectory
url: "jaywong.digital" # Leave empty for GitHub Pages, or set your custom domain

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - README.md
  - .git
  - .gitignore

# Include files/directories that start with underscores
include:
  - _layouts
  - _includes

