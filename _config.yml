# Site settings
title: "Jay Wong - Solving digital complexity with design"
description: "Product and front-end designer specialising in inclusive, AI-led and service-driven products"
permalink: /:slug/ # This creates jaywong.digital/case-study-1/ automaticallY
markdown: kramdown
highlighter: rouge

# GitHub Pages settings
# If your site is at https://username.github.io/repository-name, set baseurl to "/repository-name"
# If your site is at https://username.github.io, leave baseurl as ""
baseurl: "" # Change this if your site is in a subdirectory
url: "jaywong.digital" # Leave empty for GitHub Pages, or set your custom domain

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - README.md
  - .git
  - .gitignore
  - package.json
  - package-lock.json
  - sync.js
  - inspect.js
  - .env

# Include files/directories that start with underscores
include:
  - _layouts
  - _includes

