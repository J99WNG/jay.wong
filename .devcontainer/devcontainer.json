{
    "name": "Jekyll Site",
    "build": { "dockerfile": "Dockerfile" },
    "forwardPorts": [4000],
    "postCreateCommand": "bundle install",
    "customizations": {
      "vscode": {
        "extensions": ["jekyllrc.jekyll-syntax-support"]
      }
    }
  }